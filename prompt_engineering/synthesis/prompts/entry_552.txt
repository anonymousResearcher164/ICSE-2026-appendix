You will be provided with a behavior description of a robot, and your task is to express this description as a behavior tree in XML format. The output should be compatible with BehaviorTree.CPP.

Behavior Description:
<<<BEGIN_BEHAVIOR_DESCRIPTION>>>
The mission describes a repeated shelf-scanning and stocking-preparation task that updates the planning scene, moves to specified poses, configures perception and storage loci, and pauses before repeating.\n\nFirst, the robot will execute the following actions (TrayUpdatePlanningScene, MoveBase, MoveBase, Sequence, Pause) in order and stop the sequence if any action fails. It will run the custom action TrayUpdatePlanningScene to refresh collision and occupancy data in the planning scene. It will run the custom action MoveBase to drive to the pose at x=2.1, y=-2.0, yaw=0.0. It will run the custom action MoveBase to drive to the pose at x=2.1, y=0.0, yaw=1.57. It will execute the following actions (TorsoControl, HeadControl, ScanShelves, TraySetLoci, TrayUpdatePlanningScene) in order and stop the sequence if any action fails. It will run the custom action Pause to wait for 5 seconds before the next repeat cycle.\n\nNext, it will execute the following actions (TorsoControl, HeadControl, ScanShelves, TraySetLoci, TrayUpdatePlanningScene) in order and stop the sequence if any action fails. It will run the custom action TorsoControl to raise the torso by a relative height of 0.1 meters. It will run the custom action HeadControl to set the head pitch to 0.0 degrees and yaw to 90.0 degrees. It will run the custom action ScanShelves to detect tomato can stocking target poses and will store them in tomato_can_stocking_target_poses. It will run the custom action TraySetLoci to register loci in the tray named shelf using the stored poses and will mark those loci as unoccupied. It will run the custom action TrayUpdatePlanningScene to update the planning scene with the new loci information.\n\nFinally, it will execute Repeat for a set number of times or infinitely. It will execute the prior sequence continuously until it fails or succeeds. Else it will keep executing the sequence forever because the number of cycles is set to -1.
<<<END_BEHAVIOR_DESCRIPTION>>>
