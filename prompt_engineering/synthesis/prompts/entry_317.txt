You will be provided with a behavior description of a robot, and your task is to express this description as a behavior tree in XML format. The output should be compatible with BehaviorTree.CPP.

Behavior Description:
<<<BEGIN_BEHAVIOR_DESCRIPTION>>>
The mission describes a navigation task that plans a global path at 1 Hz and follows the path with dedicated recoveries and a higher-level recovery strategy. First, the robot will execute the following actions (RateController, RecoveryNode, RecoveryNode) in order and stop the sequence if any action fails. While an action is running or has succeeded, it will go to the next action. It will execute delayed RateController. If execution succeeds, it will start a 1.0 Hz delay timer. After the delay period is exhausted, it will resume execution. It will execute main the ROS action ComputePathToPose. If it fails, it will execute recovery action (ReactiveFallback). After recovery, it will retry main action (the ROS action ComputePathToPose). If recovery action (ReactiveFallback) fails and retries are exhausted, it will stop execution. It will execute main the ROS action FollowPath. If it fails, it will execute recovery action (ReactiveFallback). After recovery, it will retry main action (the ROS action FollowPath). If recovery action (ReactiveFallback) fails and retries are exhausted, it will stop execution. Next, it will execute main the ROS action ComputePathToPose. If it fails, it will execute recovery action (ReactiveFallback). After recovery, it will retry main action (the ROS action ComputePathToPose). If recovery action (ReactiveFallback) fails and retries are exhausted, it will stop execution. It will execute the following actions (GoalUpdated, ClearEntireCostmap) in order. If ClearEntireCostmap succeeds, it will stop execution. Else it will keep executing and re-evaluating previous actions whenever an action is running.Finally, it will execute main the ROS action FollowPath. If it fails, it will execute recovery action (ReactiveFallback). After recovery, it will retry main action (the ROS action FollowPath). If recovery action (ReactiveFallback) fails and retries are exhausted, it will stop execution. It will execute the following actions (GoalUpdated, ClearEntireCostmap) in order. If ClearEntireCostmap succeeds, it will stop execution. Else it will keep executing and re-evaluating previous actions whenever an action is running. Finally, it will execute main PipelineSequence. If it fails, it will execute recovery action (ReactiveFallback). After recovery, it will retry main action (PipelineSequence). If recovery action (ReactiveFallback) fails and retries are exhausted, it will stop execution. It will execute the following actions (GoalUpdated, RoundRobin) in order. If RoundRobin succeeds, it will stop execution. Else it will keep executing and re-evaluating previous actions whenever an action is running. It will execute the following actions (Sequence, Spin, Wait, BackUp) in order for a set number of times or infinitely. It will execute the following actions (ClearEntireCostmap, ClearEntireCostmap) in order and stop the sequence if any action fails. It will run the ROS action ClearEntireCostmap for the local costmap. It will run the ROS action ClearEntireCostmap for the global costmap. It will run the ROS action Spin with a spin distance of 1.57. It will run the ROS action Wait for 5 seconds. It will run the ROS action BackUp for a distance of 0.15 at a speed of 0.025.
<<<END_BEHAVIOR_DESCRIPTION>>>
