You will be provided with a behavior description of a robot, and your task is to express this description as a behavior tree in XML format. The output should be compatible with BehaviorTree.CPP.

Behavior Description:
<<<BEGIN_BEHAVIOR_DESCRIPTION>>>
The mission describes a social perception task that sets up pose expectations, configures indicators, and then repeatedly detects a human pose and logs evidence with feedback. First, the robot will execute the following actions (SetBlackboard, SetBlackboard, SetBlackboard, AntennaAction) in order and stop the sequence if any action fails. It will set the blackboard key pose_name_l to TouchingNeck. It will set the blackboard key pose_name_r to TouchingNeck. It will set the blackboard key pose_lr_check to any. It will run the custom action AntennaAction with rate 0, intensity 0, left_blink_pattern 0, and right_blink_pattern 0. Next, it will execute the following actions (ForceSuccess, Sequence) in order and stop the sequence if any action fails. While an action is running or has succeeded, it will go to the next action. It will execute the following actions (HumanPoseDetect, AntennaAction, SaveImageAction, TextToSpeechActionClient, AntennaAction) and make sure it always succeeds. It will run the custom action HumanPoseDetect using the expected_pose_left from pose_name_l, the expected_pose_right from pose_name_r, and the pose_lr_check from pose_lr_check while outputting detected_person, pose_left_speech, and pose_right_speech. It will run the custom action AntennaAction with rate 0, intensity 10, left_blink_pattern 10, and right_blink_pattern 10. It will run the custom action SaveImageAction to save files named image#.jpg in the directory /home/ubuntu/saved_images. It will run the custom action TextToSpeechActionClient to speak the message Saved. It will run the custom action AntennaAction with rate 0, intensity 0, left_blink_pattern 0, and right_blink_pattern 0. Finally, it will execute the following actions (Delay) in order and stop the sequence if any action fails. While an action is running or has succeeded, it will go to the next action. It will execute delayed SetBlackboard and will start the delay timer when execution succeeds. After a delay of 1000 milliseconds is exhausted, it will resume execution and set the blackboard key dummy to 0. It will execute SetBlackboard. If SetBlackboard succeeds, it will restart it. Else if SetBlackboard fails, it will stop looping and continue execution.
<<<END_BEHAVIOR_DESCRIPTION>>>
