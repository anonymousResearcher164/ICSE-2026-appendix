You will be provided with a behavior description of a robot, and your task is to express this description as a behavior tree in XML format. The output should be compatible with BehaviorTree.CPP.

Behavior Description:
<<<BEGIN_BEHAVIOR_DESCRIPTION>>>
The mission describes a grasp generation task that segments a tabletop scene, optionally isolates a desired object, and produces grasp candidates from the resulting object cloud. First, the robot will execute the following actions (RetryUntilSuccessful, ConditionalSequence, GenerateGraspingCandidates) in order and stop the sequence if any action fails. It will keep trying to execute the ROS action TableTopSegmentation until it succeeds or the attempt limit is reached. It will execute the following actions (Decorator, TableTopClustering) in order while checking a condition for each action. If condition ID is true, it will move to the next action; else if condition ID is false, it will end the sequence. It will find a value on the blackboard and check if it matches a given rule before running the ROS action ObjectCloudExtraction. It will run the ROS action TableTopClustering on the extracted or preexisting object cloud when the condition allows progression. It will run the ROS action GenerateGraspingCandidates using the available object cloud, the measured table height, and the transformed original cloud to output grasp candidates.
<<<END_BEHAVIOR_DESCRIPTION>>>
