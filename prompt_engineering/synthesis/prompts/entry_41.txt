You will be provided with a behavior description of a robot, and your task is to express this description as a behavior tree in XML format. The output should be compatible with BehaviorTree.CPP.

Behavior Description:
<<<BEGIN_BEHAVIOR_DESCRIPTION>>>
The mission describes a repeated navigation-and-task execution loop driven by temporal logic goals. First, the robot will execute the following actions (LTLPreCheck, ReactiveSequence, UpdateLTL) in order and stop the sequence if any action fails. It will run the custom action LTLPreCheck using the current temporal logic state and the desired state sequence. It will run the ReactiveSequence after the pre-check succeeds. It will run the custom action UpdateLTL to update the action sequence and to output the next navigation goal after the ReactiveSequence succeeds.\n\nNext, it will execute the following actions [Fallback, Fallback] in sequence. If any action fails, stop execution. While any action is still running, re-evaluate previous actions. It will execute the following actions (LocomotionStatusCheck, Sequence) consecutively. If any action fails, move to the next action until an action succeeds. If no action succeeds, the execution fails. It will run the custom action LocomotionStatusCheck to verify that locomotion is ready. It will execute the following actions (RecoveryStand, Delay) in order and stop the sequence if any action fails after the locomotion status check fails. It will run the custom action RecoveryStand to return to a stable stance before starting locomotion. It will execute delayed LocomotionStart and, if execution succeeds, start delay timer and, after the delay period is exhausted, resume execution. It will execute the following actions (MoveAction) consecutively. If any action fails, move to the next action until an action succeeds. If no action succeeds, the execution fails. It will run the custom action MoveAction to command motion toward the provided navigation goal while monitoring move base status.\n\nFinally, it will execute UpdateLTL for a set number of times or infinitely. It will execute UpdateLTL for the configured number of cycles as specified by num_cycles. It will keep executing the enclosing sequence for the configured number of cycles until the sequence fails or finishes all iterations.
<<<END_BEHAVIOR_DESCRIPTION>>>
