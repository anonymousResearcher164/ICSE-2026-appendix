You will be provided with a behavior description of a robot, and your task is to express this description as a behavior tree in XML format. The output should be compatible with BehaviorTree.CPP.

Behavior Description:
<<<BEGIN_BEHAVIOR_DESCRIPTION>>>
The mission describes a target-engagement task that seeks a target, moves toward it, and attacks it if adjacent, with a fallback to random movement if the main engagement fails. First, the robot will execute the following actions (GetTarget, Move, Sequence(TargetAdjacent, AttackTarget)) in sequence. If any action succeeds, it will remember it and move to the next. If any action fails, it will start the sequence again from the beginning. It will run the custom action GetTarget to obtain a target to pursue. It will run the custom action Move to advance toward the obtained target after the target acquisition succeeds. It will execute the following actions (TargetAdjacent, AttackTarget) in order and stop the sequence if any action fails. It will find a value on the blackboard and check if it matches a given rule for TargetAdjacent. It will run the custom action AttackTarget after the adjacency condition succeeds. Next, it will execute the following actions (SequenceStar(Main), RandomMove) consecutively. If any action (SequenceStar(Main)) fails, it will move to the next action until an action succeeds. If no action succeeds, the execution fails. It will run the custom action RandomMove if the main sequence does not lead to success.
<<<END_BEHAVIOR_DESCRIPTION>>>
