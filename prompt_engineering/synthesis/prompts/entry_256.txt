You will be provided with a behavior description of a robot, and your task is to express this description as a behavior tree in XML format. The output should be compatible with BehaviorTree.CPP.

Behavior Description:
<<<BEGIN_BEHAVIOR_DESCRIPTION>>>
The mission describes a repeated navigation workflow that validates a temporal-logic state, moves to a goal, and updates the state each cycle. First, the robot will execute the following actions (LTLPreCheck, MoveAction, UpdateLTL) in order and stop the sequence if any action fails. It will run the custom action LTLPreCheck using the current LTL state and the desired LTL state sequence. It will run the custom action MoveAction to navigate to the provided nav_goal while monitoring move_base_idle and move_base_finished. It will run the custom action UpdateLTL to record the executed action_sequence and to update the desired LTL state sequence and the next nav_goal after the move completes. Next, it will execute UpdateLTL, MoveAction, and LTLPreCheck in the same strict order again only if the previous cycle was restarted or continued due to success or running statuses, which does not apply here due to the basic sequence semantics. Finally, it will execute the actions for a set number of times or infinitely based on num_cycles. It will execute the sequence continuously until the configured number of repetitions is reached. It will keep executing the sequence forever if num_cycles indicates an infinite loop.
<<<END_BEHAVIOR_DESCRIPTION>>>
