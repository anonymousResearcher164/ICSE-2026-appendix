You will be provided with a behavior description of a robot, and your task is to express this description as a behavior tree in XML format. The output should be compatible with BehaviorTree.CPP.

Behavior Description:
<<<BEGIN_BEHAVIOR_DESCRIPTION>>>
The mission describes an auto-localization task that first accepts an initial pose if available and otherwise performs global localization with retries until the system becomes localized. First, the robot will execute the following actions (initialPoseReceived, doSelfLocalization) consecutively. If any action (initialPoseReceived) fails, it will move to the next action until an action succeeds. If no action succeeds, it will start all over again until an action succeeds. It will find a value on the blackboard and check if it matches a given rule for the action initialPoseReceived. It will execute the following actions (retry_client_request, retry_localization) in sequence. If any action succeeds, it will remember it and move to the next. If any action fails, it will start the sequence again from the beginning. Next, it will execute main custom action globalLocalizationServiceRequest. If it fails, it will keep trying to execute custom action globalLocalizationServiceRequest until it succeeds. If try succeeds, it will stop retrying and continue. If try fails, it will keep retrying until it succeeds. Finally, it will execute the following actions (Fallback, IsLocalized) in order and stop the sequence if any action fails. While an action is running or has succeeded, it will go to the next action. It will execute the following actions (IsLocalized, SequenceStar) consecutively. If any action (IsLocalized) fails, it will move to the next action until an action succeeds. If no action succeeds, the execution fails. It will find a value on the blackboard and check if it matches a given rule for the action IsLocalized. It will execute the following actions (Spin, BackUp, Spin) in sequence. If any action succeeds, it will remember it and move to the next. If any action fails, it will start the sequence again from the beginning. It will find a value on the blackboard and check if it matches a given rule for the action IsLocalized.
<<<END_BEHAVIOR_DESCRIPTION>>>
