You will be provided with a behavior description of a robot, and your task is to express this description as a behavior tree in XML format. The output should be compatible with BehaviorTree.CPP.

Behavior Description:
<<<BEGIN_BEHAVIOR_DESCRIPTION>>>
The mission describes a navigation task that periodically replans a global path, follows the path, and performs layered recoveries when actions fail.\n\nFirst, the robot will execute the following actions (RateController, RecoveryNode) in order and stop the sequence if any action fails. While an action is running or has succeeded, it will go to the next action. It will execute the custom action RateController to constrain downstream execution to 1.0 Hz. It will execute main the ROS action ComputePathToPose and make up to 4 retries when failures occur. If the main action fails, it will execute recovery action (ReactiveFallback) and then retry the main action until retries are exhausted.\n\nNext, it will execute the following actions (FollowPath) in order and stop the sequence if any action fails. While an action is running or has succeeded, it will go to the next action. It will execute main the ROS action FollowPath and make up to 4 retries when failures occur. If the main action fails, it will execute recovery action (ReactiveFallback) and then retry the main action until retries are exhausted.\n\nFinally, it will execute main the previous PipelineSequence and make up to 2 retries when failures occur. If the main action fails, it will execute recovery action (ReactiveFallback) and then retry the main action until retries are exhausted. It will execute the following actions [GoalUpdated, RoundRobin] in order. If GoalUpdated succeeds, it will stop execution. Else it will keep executing and re-evaluating previous actions whenever an action is running. It will execute the custom action RoundRobin to alternate between clearing the specified costmap and waiting for 5 seconds.
<<<END_BEHAVIOR_DESCRIPTION>>>
